从 @prompts.json 导入到后台prompt数据中。

要求如下

1，去重，如果参考url 和json->item>source->url一致，不要更新到后台数据库,prompt相似度大于90%也不要更新；

模型都是nano banana，生成类型：文生图，AI模型标签Banana，
卡片标题用item->title字段，详细描述，根据标题和prompt总结，标签尽可能用库中已经有的标签，如果没有就取item->tags前3个，数据库标签总体控制要少；

提示词内容item->prompts字段，图片url 是item-images数字，数组的每个内容要加上前缀"https://opennana.com/awesome-prompt-gallery/"拼接一起。然后上传R2，替换成R2的地址。

要求输出更新成功多少，失败多少，有总体的数据显示。
